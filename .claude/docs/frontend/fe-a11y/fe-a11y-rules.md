# Доступность (React + TS)

> **Что это.** Concern `fe-a11y` трека `frontend` (стек React+TS). Single-stack → плоская форма: коды + интент +
> примеры внутри. Коды: `FE-A11Y-<N>` — обязательно, `FE-A11Y-X<N>` — антипаттерн. Карта трека — `frontend/_index.md`.
>
> **Статус: STUB (каркас).** Направление ниже задано, наполняет **FE-лид** по `_meta/authoring-contract.md` §8.
> После наполнения — создать пару `ucp-fe-a11y-{design,review}` (track: frontend) и
> прогнать `ucp-meta-review`. Зарегистрировать `FE-A11Y-*` в `_meta/rule-code-registry.md`.

**Интент (что покрывает):** семантический HTML (button — это <button>); ARIA только когда семантики не хватает; фокус управляем и видим; клавиатурная навигация; контраст; формы с label (web.dev-гайдлайны).

## 1. Семантика и ARIA
**MUST:**
- **FE-A11Y-10.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-A11Y-X1.** [TODO: FE-лид] …

## 2. Фокус и клавиатура
**MUST:**
- **FE-A11Y-20.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-A11Y-X2.** [TODO: FE-лид] …

## 3. Контраст и формы
**MUST:**
- **FE-A11Y-30.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-A11Y-X3.** [TODO: FE-лид] …
