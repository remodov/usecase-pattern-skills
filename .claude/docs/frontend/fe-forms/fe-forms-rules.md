# Формы и валидация (React + TS)

> **Что это.** Concern `fe-forms` трека `frontend` (стек React+TS). Single-stack → плоская форма: коды + интент +
> примеры внутри. Коды: `FE-FORM-<N>` — обязательно, `FE-FORM-X<N>` — антипаттерн. Карта трека — `frontend/_index.md`.
>
> **Статус: STUB (каркас).** Направление ниже задано, наполняет **FE-лид** по `_meta/authoring-contract.md` §8.
> После наполнения — создать пару `ucp-fe-forms-{design,review}` (track: frontend) и
> прогнать `ucp-meta-review`. Зарегистрировать `FE-FORM-*` в `_meta/rule-code-registry.md`.

**Интент (что покрывает):** react-hook-form (неконтролируемые по умолчанию); схемная валидация (zod) — единый источник правил; submit идемпотентен/disabled во время отправки; серверные ошибки маппятся на поля.

## 1. Контролируемость и перформанс
**MUST:**
- **FE-FORM-10.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-FORM-X1.** [TODO: FE-лид] …

## 2. Схемная валидация (zod)
**MUST:**
- **FE-FORM-20.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-FORM-X2.** [TODO: FE-лид] …

## 3. Submit, серверные ошибки на поля
**MUST:**
- **FE-FORM-30.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-FORM-X3.** [TODO: FE-лид] …
