# Роутинг (React + TS)

> **Что это.** Concern `fe-routing` трека `frontend` (стек React+TS). Single-stack → плоская форма: коды + интент +
> примеры внутри. Коды: `FE-RT-<N>` — обязательно, `FE-RT-X<N>` — антипаттерн. Карта трека — `frontend/_index.md`.
>
> **Статус: STUB (каркас).** Направление ниже задано, наполняет **FE-лид** по `_meta/authoring-contract.md` §8.
> После наполнения — создать пару `ucp-fe-routing-{design,review}` (track: frontend) и
> прогнать `ucp-meta-review`. Зарегистрировать `FE-RT-*` в `_meta/rule-code-registry.md`.

**Интент (что покрывает):** декларативные маршруты; code-splitting по роуту (lazy); guard'ы по auth/роли на границе роута; параметры типизированы; нет бизнес-логики в роут-компоненте.

## 1. Структура маршрутов
**MUST:**
- **FE-RT-10.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-RT-X1.** [TODO: FE-лид] …

## 2. Code-splitting / lazy
**MUST:**
- **FE-RT-20.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-RT-X2.** [TODO: FE-лид] …

## 3. Guard'ы по auth/роли
**MUST:**
- **FE-RT-30.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-RT-X3.** [TODO: FE-лид] …
