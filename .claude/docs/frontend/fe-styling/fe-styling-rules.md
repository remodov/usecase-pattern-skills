# Стили и дизайн-система (React + TS)

> **Что это.** Concern `fe-styling` трека `frontend` (стек React+TS). Single-stack → плоская форма: коды + интент +
> примеры внутри. Коды: `FE-STY-<N>` — обязательно, `FE-STY-X<N>` — антипаттерн. Карта трека — `frontend/_index.md`.
>
> **Статус: STUB (каркас).** Направление ниже задано, наполняет **FE-лид** по `_meta/authoring-contract.md` §8.
> После наполнения — создать пару `ucp-fe-styling-{design,review}` (track: frontend) и
> прогнать `ucp-meta-review`. Зарегистрировать `FE-STY-*` в `_meta/rule-code-registry.md`.

**Интент (что покрывает):** дизайн-токены (цвета/отступы/типографика) — единый источник; компоненты из дизайн-системы; выбранный CSS-подход един по проекту; нет магических инлайн-значений.

## 1. Дизайн-токены
**MUST:**
- **FE-STY-10.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-STY-X1.** [TODO: FE-лид] …

## 2. Компоненты дизайн-системы
**MUST:**
- **FE-STY-20.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-STY-X2.** [TODO: FE-лид] …

## 3. CSS-подход (единый)
**MUST:**
- **FE-STY-30.** [TODO: FE-лид] …
**MUST NOT:**
- **FE-STY-X3.** [TODO: FE-лид] …
