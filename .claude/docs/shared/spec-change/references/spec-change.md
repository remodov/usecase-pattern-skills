Скелет документа изменения к требованиям `../spec.md`. Копируется в
`docs/spec/changes/<YYYY-MM-DD>-<slug>.md`, один файл — одно изменение;
**эти три строки при копировании убираются**, frontmatter идёт первым.

---
context: <service>
change: <короткое-имя>
status: proposed
level: <0|1|2|3>
---

# Изменение: <что меняем, одной фразой>

## 1. Зачем

<Проблема или требование своими словами. Что ломается сегодня и во что это
обходится. Какие альтернативы рассмотрены и почему выбрана эта. Это
единственное место, где «почему» останется после слияния.>

## 2. Что меняется

| Файл | Раздел | Было | Станет |
| --- | --- | --- | --- |

<Строка на каждый затронутый раздел. Ссылка на раздел — по имени, не по номеру.>

## 3. Влияние

**Класс**: <compatible | breaking | semantic-break>

| Что затронуто | Кто потребитель | Как узнает |
| --- | --- | --- |

<Поимённо: Use Case, команды, события, контракты. Сомнение между compatible
и semantic-break решается в пользу semantic-break.>

## 4. Миграция

<Стратегия под класс: расширение с последующим сжатием для ломающего контракта,
версия события рядом со старой, договорённость и уведомление для смены смысла.
Для compatible — строка «не требуется».>

## 5. Задачи

<Вертикальными срезами: каждый собирается и проверяется отдельно и называет
конкретный ucp-скилл.>

- [ ] Срез 1: <что сквозным куском> — `/ucp-<...>`
- [ ] Срез 2: <…> — `/ucp-<...>`

## 6. Критерии приёмки

- **Дано** <состояние> · **Когда** <действие> · **Тогда** <исход>
- **Дано** <состояние> · **Когда** <ошибочный путь> · **Тогда** <как отказывает>

<При слиянии переезжают в «Критерии приёмки» спеки.>

## 7. Слияние

- [ ] все разделы из «Что меняется» обновлены в спеке
- [ ] критерии приёмки переехали в спеку
- [ ] глоссарий пополнен новыми терминами
- [ ] `status: merged`, дата <YYYY-MM-DD>
- [ ] файл переехал в `changes/archive/`
