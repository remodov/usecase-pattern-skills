Скелет файла домен-юнита к требованиям `../spec.md`. Копируется в
`docs/spec/aggregates/<unit>.md` — по файлу на юнит, даже если он один;
**эти три строки при копировании убираются**, frontmatter идёт первым.

---
context: <service>
aggregate: <Name>
level: 0
---

# <Name>

## О юните

<Две-три строки: что моделирует юнит, как он живёт, что в нём неочевидно.>

## Важно знать

- <главный инвариант юнита>
- <переход, которого не бывает, и почему>
- <кто владеет данными юнита и кто только читает>
- <неочевидное: что считается фоном, что синхронно>

## 1. Доменная модель

<Вводная фраза: из чего состоит юнит.>

| Элемент | Вид (корень / сущность) | За что отвечает |
| --- | --- | --- |

Value object описывается инвариантом, который защищает, а не полями:

| Value object | Какой инвариант держит |
| --- | --- |

Хранение — в разделе «Техническая реализация» корневого файла.

## 2. Жизненный цикл

<Вводная фраза: какие состояния бывают и что их меняет.>

| Из состояния | В состояние | Триггер (команда / событие / политика) |
| --- | --- | --- |

## 3. Доступ

<Вводная фраза: кому что позволено. Роли определены в разделе 4 корневого файла.>

| Операция | Роль | Ограничение по данным |
| --- | --- | --- |

## 4. Бизнес-правила

<Вводная фраза: какие правила юнит держит.>

| Код | Формулировка | Что ломается, если нарушить | Команда | Ошибка |
| --- | --- | --- | --- | --- |

<Колонка последствия — в терминах бизнеса и данных: «клиент получает вторую
доставку за свой счёт», а не «нарушается инвариант». Тип правила — инвариант,
предусловие, постусловие — если нужен, идёт отдельной колонкой или пометкой
в формулировке.>

Код — с префиксом этого домен-юнита, чтобы не сталкивался с соседними.

## 5. Команды

<Вводная фраза: что актор может сделать с юнитом. Реакции на события
и политики по таймауту сюда не выносятся — они в матрице переходов.>

### <ИмяКоманды>

- **Переход**: <из состояния → в состояние>
- **Вход**: <что принимает>
- **Предусловия**: <что должно быть верно>
- **Логика**: <что происходит>
- **Эмитит**: <какие события>
- **Ошибки**: <коды>

## 6. Доменные события

<Вводная фраза: о чём юнит сообщает.>

| Событие | Триггер | Scope (внутреннее / внешнее) | Подписчики |
| --- | --- | --- | --- |

## 7. Запросы

<Вводная фраза: на какие вопросы отвечает юнит.>

### <ИмяЗапроса>

- **Вопрос**: <на что отвечает>
- **Параметры**: <что принимает>
- **Возвращает**: <что отдаёт>
- **Логика**: <источник чтения, фильтр, согласованность>

## Что не определено

| Раздел | Чего нет | Чем закрыть |
| --- | --- | --- |

## Частые ошибки

<То, обо что спотыкаются именно в этом юните. Общие советы по разработке сюда
не пишутся.>

| Ошибка | Что ломается | Как правильно |
| --- | --- | --- |

## Куда дальше

- `<соседний-юнит>.md` — <что там найдётся: чей статус, какое правило>
- Корневой файл, «Доменные события» — <какие события юнита уходят наружу>
- <контракт> — <что описывает>
