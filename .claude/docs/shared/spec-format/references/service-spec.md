Скелет корневого файла спеки к требованиям `../spec.md`. Копируется в
`docs/spec/<service>-spec.md`; **эти три строки при копировании убираются** —
frontmatter обязан быть первой строкой файла. Угловые скобки — места автора.

---
context: <service>
bounded-context: <BoundedContext>
level: 0
---

# Use Case спецификация: <service> (Уровень N)

## О сервисе

<Пять-десять строк прозой, три абзаца. Первый — чем занимается сервис своими
словами. Второй — два-три главных потока: кто инициирует, что происходит, чем
заканчивается. Третий — две-три особенности или риска, обо что спотыкаются:
отложенная согласованность, ручные операции, привязка к операционному дню,
бюджет ответа. Без таблиц, кодов правил и ссылок на источники.>

## Важно знать

<Пять-семь строк простым языком, каждая — отдельный буллет. Что обязано быть
верно всегда, чем сервис отличается от соседей, обо что спотыкаются. Прочитавший
только эту врезку принимает верные решения в девяти случаях из десяти.
Перечисление разделов сюда не годится.>

- <главный инвариант домена одной фразой>
- <что здесь согласовано отложенно и чем это грозит>
- <что делается вручную и кем>
- <жёсткое ограничение: бюджет ответа, окно операционного дня, лимит>
- <самая частая ошибка тех, кто приходит в этот сервис впервые>

## 1. Bounded Context

<Вводная фраза: где проходит граница и почему здесь.>

Контекст · субдомен · владелец · миссия — строками. Дальше таблица домен-юнитов
со ссылками на файлы, что внутри границы и что вне, стыки с соседями.

| Домен-юнит | Файл | За что отвечает |
| --- | --- | --- |

## 2. Интеграции (Context Map)

<Вводная фраза: с кем сервис разговаривает и какие рёбра критичны.>

Диаграмма карты контекста, затем рёбра. Значения — из закрытого словаря:
направление `inbound | outbound | bidirectional`, канал `sync | async |
sync+async`, связь `customer-supplier | conformist | ohs | acl |
published-language | shared-kernel`.

| Сосед | Направление | Канал | Связь | Контракт | Evidence |
| --- | --- | --- | --- | --- | --- |

## 3. Ubiquitous Language

<Вводная фраза: какие термины здесь свои и что с чем не путать.>

| Термин | Код | Определение |
| --- | --- | --- |

## 4. Роли и доступ

<Вводная фраза: кто действует в контексте.>

| Роль | Кто это | Общие ограничения |
| --- | --- | --- |

Доступ к операциям — в разделе «Доступ» каждого домен-юнита.

## 5. Доменные события

<Вводная фраза: что контекст обещает наружу.>

| Домен-юнит | Внешнее событие | Канал |
| --- | --- | --- |

Полное описание — у домен-юнита.

## 6. Use Cases

<Вводная фраза: какие сквозные сценарии здесь описаны.>

### UC-1 <Имя сценария>

- **Актор**: <кто>
- **Домен-юниты**: <какие затронуты>
- **Основной поток**: <шаги>
- **Альтернативы**: <ветки и отказы>

## 7. Процессы

<Вводная фраза: какие процессы пересекают домен-юниты. Нет таких — пометка
«не применимо» с одной строкой почему.>

## 8. UI-спецификация

<Вводная фраза: как состояния показываются пользователю.>

| Состояние | Что видит пользователь | Текст ошибки |
| --- | --- | --- |

## 9. Критерии приёмки

<Вводная фраза: что проверяется на приёмке.>

- **Дано** <исходное состояние> · **Когда** <действие> · **Тогда** <исход>

## 10. Нефункциональные требования

<Вводная фраза: какие пороги обязательны и откуда они взялись.>

| Свойство | Целевое значение | Условие измерения |
| --- | --- | --- |

## 11. Техническая реализация

<Вводная фраза: единственный технический раздел спеки.>

Слои и стек, схема данных, затем два обязательных реестра.

### Каналы и топики

<Вводная фраза: с кем сервис разговаривает по этим каналам.>

| Канал/топик | Направление (consume/produce) | Брокер/кластер | Consumer group / producer-id | Тип сообщения (payload) | Evidence (файл:строка) |
| --- | --- | --- | --- | --- | --- |

Канал в обе стороны — две строки. Брокеров нет — вместо таблицы пометка
«не применимо: брокеров нет».

### REST-поверхность

<Вводная фраза: что сервис отдаёт наружу.>

| Метод | Путь | operationId | Назначение | Evidence |
| --- | --- | --- | --- | --- |

REST нет — пометка «не применимо: REST-endpoint'ов нет».

## Что не определено

<Вводная фраза: чего в источнике не нашлось.>

| Раздел | Чего нет | Чем закрыть |
| --- | --- | --- |

Пробелов нет — строка «пробелов нет: все обязательные поля заполнены».
