---
name: ucp-spec-tier-0
description: Сгенерировать as-is Use Case спецификацию существующего сервиса из его кода — без бизнес-брифа, без интервью. На входе только репо (Java, миграции, application.yml, OpenAPI, тесты); на выходе — спека в том же формате, что у `ucp-spec-design`, с флагом `source: code` и выведенным уровнем зрелости; содержимое снимает факт «как сейчас», отсутствующие данные помечаются литералом `not-declared`. Для онбординга сервиса не по UCP и как точка отсчёта для миграции на уровень 1/2/3.
allowed-tools: Read Glob Grep Write Edit Bash(./gradlew*) Bash(mvn*) Skill(superpowers:*)
---

# Use Case спецификация — as-is реверс из кода

Ты пишешь **as-is спецификацию** существующего сервиса — снимок «как сейчас» в **том же формате**, что и `ucp-spec-design` (см. `.claude/docs/usecase-spec-template.md`, **прочитай первым делом**). Содержимое каждой секции — факт из кода; чего в репо нет — литерал `not-declared`.

As-is нужен для онбординга (структурированное описание вместо россыпи README) и как база для миграции (`not-declared`-поля подсвечивают, где сервис не следует UCP).

Скилл **не классифицирует** код как правильный/неправильный, **не предлагает рефакторинг**, **не выдумывает агрегаты, акторов, цели команд или типы Context Mapping** там, где их нет в коде. Фиксирует факт, иначе `not-declared`.

## Зависимости

- Плагин `superpowers` — TodoWrite на крупных сервисах (не обязателен).
- Шаблон `.claude/docs/usecase-spec-template.md` — обязательный источник формата. Нет — остановись.
- `context7` и живая БД **не используются** — источник только репо.

## Инструкции

1. **Прочитай формат** из `.claude/docs/usecase-spec-template.md`.

2. **Имя сервиса** — из `build.gradle`/`pom.xml` (`rootProject.name`/`artifactId`), README или директории. Неоднозначно — спроси.

3. **Маркер as-is.** В заголовке — `(as-is)`, во frontmatter — `source: code`. As-is — это ingest из кода, не уровень зрелости; уровень (`level: 1|2|3`) выводится из кода отдельно (есть `usecase-pattern`/агрегаты/порты — Уровень 2/3, иначе Уровень 1) и тоже кладётся во frontmatter.

4. **Раскладка — всегда корень + `aggregates/`.** Корень `docs/spec/<service>-spec.md` (контекст) + `docs/spec/aggregates/<unit>.md` на каждый домен-юнит. На Уровне 3 юниты = агрегаты из кода (`AggregateRoot`); для anemic-кода (Уровень 1–2, агрегатов нет) — центральная сущность(и) из таблиц, всё равно отдельным файлом. frontmatter: `context` + `bounded-context` + `level` + `source: code` (корень); `context` + `aggregate` + `level` (юнит).

5. **Собери источники из репо → секции** (нумерация и имена — из шаблона):

   | Источник | Секция |
   |---|---|
   | README (назначение, границы) | Bounded Context; НФТ |
   | `build.gradle*`/`pom.xml`, `application.yml` | Техническая реализация (стек); Интеграции |
   | Миграции (Flyway/Liquibase), `@Entity`/`@Table` | Техническая реализация → «Схема БД» (ER, FK, индексы); список таблиц — в Доменную модель |
   | `enum`-статусы + поля `status` | Жизненный цикл |
   | `SecurityConfig`, `@PreAuthorize`, JWT | Роли и доступ |
   | `@Valid`/JSR-303, БД-`CHECK`/`UNIQUE`, `if(...) throw` | Бизнес-правила |
   | POST/PUT/PATCH/DELETE-endpoint, `@KafkaListener` | Команды |
   | GET-endpoint | Запросы |
   | `KafkaTemplate.send`/`@SendTo` | Доменные события (+ publish-рёбра в Интеграции) |
   | `@ExceptionHandler`/наследники `RuntimeException` | коды ошибок — в поле «Ошибки» команд (каталога ошибок нет) |
   | OpenAPI/Swagger | UI / контракты на рёбрах Интеграции |
   | DataSource/Kafka/`@FeignClient`/S3 из `application.yml` | Интеграции (рёбра) |
   | Интеграционные тесты `*IT.java`, `@SpringBootTest` | Use Cases; Критерии приёмки (GWT из `@DisplayName`) |
   | `@Transactional` поверх неск. ресурсов, `*_outbox`/`event_log`, `@Scheduled` over aggregates | Процессы (Saga) |

6. **Соблюдай тот же формат, что `ucp-spec-design`:** домен/техника разделены (схема БД, стек, Outbox, топики → только §Техническая реализация), команды/запросы — карточки, события — таблицей у агрегата + контракт в корне (при split), интеграции — нормализованный словарь, ссылки — якорные. Карточек-файлов на сущность/команду **не создаём** (это старый формат).

7. **Все секции присутствуют всегда.** Если в коде нет, например, `KafkaTemplate.send` — раздел «Доменные события» содержит пометку «Сервис не публикует событий (`KafkaTemplate.send`/`@SendTo` не найдены)», а не молчаливое опущение.

8. **Литерал `not-declared` — обязательный** для отсутствующих данных (актор команды, идемпотентность, тип Context Mapping, SLA, инварианты агрегата, владелец). Не пустая строка, не `null`, не TODO. Для anemic-модели §Доменная модель помечает «агрегаты в коде не выделены (anemic); модель = таблицы, см. Техническая реализация → Схема БД».

9. **Реконструированные факты помечай источником:** `(reconstructed-from: OrderService.java:142)` — чтобы читатель проверил.

10. **Код не пиши.** Только markdown-спека. Никаких рефакторингов и `// TODO migrate` в исходниках.

11. **Самопроверка:**
    - Заголовок несёт `(as-is)`; раскладка — корень + `aggregates/<unit>.md`; frontmatter несёт `level` и `source: code`.
    - Все секции созданы; пустые — с явной пометкой об отсутствии.
    - Техника (схема БД, стек, топики) — только в §Техническая реализация; §1–§10 без неё.
    - Отсутствующие данные = `not-declared`; реконструкции помечены `(reconstructed-from: …)`.
    - Не выдуманы бизнес-факты (акторы, цели, типы связи), которых нет в коде.

12. **Структура вывода:**
    1. Резюме: имя сервиса, статистика (N таблиц, M команд, K событий, L ошибок, P интеграций), `(as-is, source: code)`, выведенный уровень зрелости, источники.
    2. Дерево созданных файлов в `docs/spec/`.
    3. **Список явных gap-ов** (`not-declared`-поля по разделам) — что в коде не выражено и требует уточнения при миграции на уровень 1/2/3.
    4. Следующие шаги: as-is спека читается downstream-review-скиллами; при миграции команда заменяет `not-declared` на реальные значения.

$ARGUMENTS
